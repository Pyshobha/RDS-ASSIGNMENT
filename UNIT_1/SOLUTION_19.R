original_vector <- c(-2, 4, -6, 8, -10)
repeated_vector <- rep(original_vector, times = 3)
each_repeated_vector <- rep(original_vector, each = 5)
sorted_repeated_vector <- sort(repeated_vector)
sorted_each_repeated_vector <- sort(each_repeated_vector)
repeated_vector
each_repeated_vector
sorted_repeated_vector
sorted_each_repeated_vector

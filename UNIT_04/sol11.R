# 11. Create a nested list where one element is itself a list containing a numeric vector and a character vector:

nested_list <- list(inner_list = list(numbers = c(1, 2, 3), letters = c("a", "b", "c")))
